version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd sarathi-frontend
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: sarathi-frontend/dist
    files:
      - '**/*'
  cache:
    paths:
      - sarathi-frontend/node_modules/**/*
